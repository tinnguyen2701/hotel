export enum RoomStatus {
    All = 1,
    Available = 2,
    Checkin = 3,
    Booking = 4,
}

export enum RoomType {
    Single = 1,
    Double = 2
}

export enum ActionType {
    None = 0,
    Checkin = 1,
    Checkout = 2,
    Edit = 3,
}
