import * as faker from "faker";
import { random } from "lodash";
//
import { CustomerModel } from "@app/modules/admin/models/room.model";

export function getCustomerWithBookCodeId(): CustomerModel[] {
    return [
        new CustomerModel({

        })
    ]
}
